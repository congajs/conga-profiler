services:

    # Security Authenticator #
    profiler.security.authenticator:
        constructor: "@conga/framework-profiler:security/ProfilerAuthenticator"
        arguments: ["@service_container"]


    # Security Provider #
    profiler.security.provider:
        constructor: "@conga/framework-profiler:security/ProfilerProvider"
        arguments: ["@service_container"]