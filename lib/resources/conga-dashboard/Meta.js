const package = require('../../../package.json');

module.exports = {

    id: 'profiler',
    name: 'Profiler',
    description: 'the request profiler',
    bundle: package.name,
    version: package.version,
    icon: 'clock-o'

}
